# -*- encoding: utf-8 -*-

Gem::Specification.new do |s|
  s.name = "abstract"
  s.version = "1.0.0"

  s.required_rubygems_version = nil if s.respond_to? :required_rubygems_version=
  s.authors = ["makoto kuwata"]
  s.cert_chain = nil
  s.date = "2006-03-12"
  s.description = "'abstract.rb' is a library which enable you to define abstract method in Ruby."
  s.homepage = "http://rubyforge.org/projects/abstract"
  s.require_paths = ["lib"]
  s.required_ruby_version = Gem::Requirement.new("> 0.0.0")
  s.rubygems_version = "1.8.15"
  s.summary = "a library which enable you to define abstract method in Ruby"

  if s.respond_to? :specification_version then
    s.specification_version = 1

    if Gem::Version.new(Gem::VERSION) >= Gem::Version.new('1.2.0') then
    else
    end
  else
  end
end
