<%= @app.name %> was just deployed to <%= @deploy.environment %> by <%= @deploy.username %>.

Details:

        What: <%= @app.name %><%= "@#{@deploy.revision}" unless @deploy.revision.blank? %>
        When: <%= @deploy.created_at.to_s(:rfc822) %>
  Repository: <%= @deploy.repository.blank? ? 'n/a' : @deploy.repository %>

