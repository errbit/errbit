<%= @app.name %> was just deployed to <%= @deploy.environment %> by <%= @deploy.username %>.


REVISION:

<%= @deploy.revision %>


WHEN:

<%= @deploy.created_at.to_s(:rfc822) %>


REPOSITORY:

<%= @deploy.repository.blank? ? 'n/a' : @deploy.repository %>

