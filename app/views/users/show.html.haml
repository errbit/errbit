- content_for :title, @user.name

%table
  %tr
    %th Email
    %td.main= @user.email
  %tr
    %th Admin?
    %td= @user.admin? ? 'Y' : 'N'
  %tr
    %th Created
    %td= @user.created_at.to_s(:micro)