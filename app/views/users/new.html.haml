- content_for :title, 'New User'
- content_for :action_bar, link_to('cancel', users_path, :class => 'btn btn-default')

= form_for user do |f|

  = render 'fields', :f => f

  %div.buttons= f.submit 'Add User', :class => "btn btn-success"
