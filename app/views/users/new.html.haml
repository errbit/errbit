- content_for :title, t('.new_user')
- content_for :action_bar, link_to(t('cancel'), users_path, :class => 'btn btn-default')

= form_for user do |f|
  = render 'fields', :f => f

  %div.buttons= f.submit t('.add_user'), class: 'btn btn-primary'
