# frozen_string_literal: true

require "rails_helper"

RSpec.describe WatchersController, type: :request do
  describe "#create" do
    context "when user is logged in" do

    end

    context "when user is not logged in" do

    end
  end

  describe "#destroy" do
    context "when user is logged in" do

    end

    context "when user is not logged in" do

    end
  end
end
