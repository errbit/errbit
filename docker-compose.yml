version: '2.1'
errbit:
  build: .
  image: 127.0.0.1:30000/formlabs/formerrbit
  ports:
    - "8080:8080"
  links:
    - mongo
  environment:
    - PORT=8080
    - RACK_ENV=production
    - MONGO_URL=mongodb://mongo:27017/errbit
mongo:
  image: mongo:3.2
  ports:
    - "27017"
