services:
  errbit:
    build: .
    environment:
      RAILS_ENV: production
      SECRET_KEY_BASE: "${SECRET_KEY_BASE:?SECRET_KEY_BASE must be set}"
      ERRBIT_HOST: errbit.example.com
    ports:
      - "3000:80"
    volumes:
      - "./storage:/rails/storage:rw"
